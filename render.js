
import {state,TYPE_LABELS,esc} from './state.js';import {getUrl} from './media.js';
export function appTemplate(){return `<div class="splash" id="splash"><img src="assets/boonwave-mark-full.png"></div><section id="auth"></section><section id="shell" class="shell hidden"></section><div id="notice" class="notice hidden"></div>`}
export function authTemplate(){return `<div class="auth-screen"><div class="auth-card"><img class="auth-logo" src="assets/boonwave-mark-full.png"><div class="auth-tabs"><button data-auth="login" class="${state.ui.authMode==='login'?'active':''}">Вход</button><button data-auth="register" class="${state.ui.authMode==='register'?'active':''}">Регистрация</button></div><form id="authForm"><div class="field"><label>Email</label><input name="email" type="email" required placeholder="name@example.com"></div><div class="field"><label>Пароль</label><input name="password" type="password" required minlength="4"></div><div class="auth-actions"><button class="primary">${state.ui.authMode==='login'?'Войти':'Создать аккаунт'}</button><button type="button" class="ghost" id="demoEnter">Визуальное знакомство</button></div></form></div></div>`}
export function shellTemplate(){return `<header class="topbar"><button class="logo-btn" id="logoBtn"><img src="assets/icon-192.png"></button><div class="space-tabs"><button data-space="personal" class="${state.space==='personal'?'active':''}">Личное</button><button data-space="work" class="${state.space==='work'?'active':''}">Проекты</button></div><button class="round-btn" id="menuBtn">⋮</button></header><div id="viewport" class="viewport"><div id="world" class="world"><div class="grid"></div><svg id="links" class="links"></svg><div id="nodes"></div><div id="cardActions" class="card-actions"><button data-action="level" title="Изменить масштаб">◉</button><button data-action="link" title="Связать">🔗</button><button data-action="lock" title="Блокировка">♙</button></div></div></div><div class="right-tools"><button id="fitBtn">⌁</button><button id="focusBtn">⤢</button></div><div class="bottom-ui"><div class="zoom-row"><button class="now-btn" id="nowBtn">♮</button><div class="zoom-pill"><span>−</span><input id="zoomRange" type="range" min="0.35" max="1.8" step="0.01" value="1"><span>＋</span></div><button class="create-btn" id="createBtn">＋</button></div><nav class="bottom-nav"><button data-create="person"><b>♙</b>Человек</button><button data-create="idea"><b>♧</b>Идея</button><button data-create="goal"><b>◎</b>Цель</button><button id="archiveBtn"><b>▣</b>Архив</button></nav></div><div id="modalRoot"></div><div id="linkMenuRoot"></div>${state.data.settings.hintDismissed?'':`<div class="hint" id="hint"><ul><li>Перемещайте поле одним пальцем</li><li>Масштабируйте двумя пальцами</li><li>Двойное касание открывает карточку</li><li>Удержание 1,2 сек — редактирование</li></ul><button class="ghost" id="hintClose" style="width:100%">Понятно</button></div>`}`}
function cardIcon(type){return {project:'▣',process:'⌘',person:'♙',idea:'♧',goal:'◎'}[type]||'•'}
export function nodeHtml(n){const cover=n.coverAssetId?`<img data-cover="${esc(n.coverAssetId)}">`:`<div class="card-icon">${cardIcon(n.type)}</div>`;const metrics=n.type==='project'?`<span class="chip">${n.positions||0}</span><span class="chip">${n.progress||0}%</span>`:n.type==='process'?`<span class="chip">${(n.tasks||[]).length} задач</span><span class="chip">${n.progress||0}%</span>`:n.type==='goal'?`<span class="chip">${n.progress||0}%</span>`:'';return `<article class="node level-${n.level||2} ${state.selectedId===n.id?'selected':''}" data-id="${n.id}" data-type="${n.type}" style="left:${n.x}px;top:${n.y}px"><div class="card"><div class="card-cover">${cover}</div><span class="card-pill">${TYPE_LABELS[n.type]}</span><span class="status-dot"></span><div class="card-body"><h3 class="card-title">${esc(n.title||TYPE_LABELS[n.type])}</h3><div class="card-sub">${esc(subtitle(n))}</div><div class="card-metrics">${metrics}</div></div></div></article>`}
function subtitle(n){return n.type==='project'?(n.client||'Проект'):n.type==='process'?`${(n.stages||[]).length} этапа · ${(n.tasks||[]).length} задач`:n.type==='person'?(n.speciality||'Специалист'):n.type==='idea'?(n.source||'Идея'):n.type==='goal'?(n.metric||'Цель'):''}
export async function hydrateCovers(root){for(const img of root.querySelectorAll('[data-cover]')){const u=await getUrl(img.dataset.cover).catch(()=>null);if(u&&img.isConnected)img.src=u}}
export function detailHtml(n){const contacts=n.type==='person'?personContacts(n):'';return `<div class="overlay" data-close-modal><section class="sheet" data-modal><header class="sheet-head"><div><small>${TYPE_LABELS[n.type]}</small><h2>${esc(n.title)}</h2></div><button data-close>×</button></header><div class="sheet-body">${n.coverAssetId?`<div class="hero"><img data-cover="${n.coverAssetId}"><div class="hero-copy"><h3>${esc(n.title)}</h3><p>${esc(subtitle(n))}</p></div></div>`:''}<div class="detail-grid"><div class="stat"><small>ТИП</small><b>${TYPE_LABELS[n.type]}</b></div><div class="stat"><small>СТАТУС</small><b>${esc(n.personStatus||n.status||'Активно')}</b></div></div>${contacts}${n.note?`<section class="section"><h3>Заметка</h3><div class="stat">${esc(n.note)}</div></section>`:''}${linksSection(n)}</div><footer class="sheet-footer"><button class="ghost" data-archive-node>В архив</button><button class="primary" data-edit-node>Редактировать</button></footer></section></div>`}
function personContacts(n){const rows=[];if(n.phone)rows.push(row('☎','Позвонить',n.phone,`tel:${n.phone}`));if(n.telegram)rows.push(row('➤','Telegram',n.telegram,n.telegram.startsWith('@')?`https://t.me/${n.telegram.slice(1)}`:`https://t.me/${n.telegram}`));if(n.email)rows.push(row('✉','Email',n.email,`mailto:${n.email}`));if(n.site)rows.push(row('↗','Сайт',n.site,n.site.startsWith('http')?n.site:`https://${n.site}`));if(n.address)rows.push(row('⌖','Адрес',n.address,`https://maps.apple.com/?q=${encodeURIComponent(n.address)}`));return rows.length?`<section class="section"><h3>Контакты</h3><div class="click-list">${rows.join('')}</div></section>`:''}
function row(i,t,v,h){return `<a class="click-row" href="${esc(h)}" target="${h.startsWith('http')?'_blank':'_self'}"><span class="click-icon">${i}</span><span><b>${esc(t)}</b><small>${esc(v)}</small></span><span>›</span></a>`}
function linksSection(n){const related=state.data.links.filter(l=>l.from===n.id||l.to===n.id).map(l=>state.data.nodes.find(x=>x.id===(l.from===n.id?l.to:l.from))).filter(Boolean);return related.length?`<section class="section"><h3>Связан с</h3><div class="click-list">${related.map(x=>`<button class="click-row" data-open-related="${x.id}"><span class="click-icon">${cardIcon(x.type)}</span><span><b>${esc(x.title)}</b><small>${TYPE_LABELS[x.type]}</small></span><span>›</span></button>`).join('')}</div></section>`:''}
export function editorHtml(n){return `<div class="overlay" data-close-modal><section class="sheet" data-modal><header class="sheet-head"><div><small>${TYPE_LABELS[n.type]} · редактирование</small><h2>${esc(n.title||'Новая карточка')}</h2></div><button data-close>×</button></header><form id="editForm"><div class="sheet-body"><div class="form-grid"><div class="field"><label>Название</label><input name="title" value="${esc(n.title||'')}" required></div><div class="field"><label>Фото / обложка</label><div class="media-preview" id="mediaPreview">${n.coverAssetId?`<img data-cover="${n.coverAssetId}">`:'Нет изображения'}</div><button type="button" class="ghost" data-pick-media>Добавить / заменить фото</button></div>${fieldsFor(n)}<div class="field"><label>Заметка</label><textarea name="note">${esc(n.note||'')}</textarea></div></div></div><footer class="sheet-footer"><button type="button" class="danger" data-delete-node>Удалить</button><button class="primary">Сохранить</button></footer></form></section></div>`}
function fieldsFor(n){if(n.type==='person')return `<div class="field"><label>Специализация</label><input name="speciality" value="${esc(n.speciality||'')}"></div><div class="field"><label>Статус</label><select name="personStatus"><option>Новый контакт</option><option>На связи</option><option>Перспективный</option><option>Работаем</option><option>Пауза</option><option>Важный человек</option></select></div><div class="two"><div class="field"><label>Телефон</label><input name="phone" value="${esc(n.phone||'')}"></div><div class="field"><label>Telegram</label><input name="telegram" value="${esc(n.telegram||'')}"></div></div><div class="two"><div class="field"><label>Email</label><input name="email" value="${esc(n.email||'')}"></div><div class="field"><label>Сайт</label><input name="site" value="${esc(n.site||'')}"></div></div><div class="field"><label>Адрес</label><input name="address" value="${esc(n.address||'')}"></div>`;if(n.type==='project')return `<div class="two"><div class="field"><label>Клиент</label><input name="client" value="${esc(n.client||'')}"></div><div class="field"><label>Прогресс</label><input name="progress" type="number" min="0" max="100" value="${n.progress||0}"></div></div>`;if(n.type==='process')return `<div class="field"><label>Прогресс</label><input name="progress" type="number" min="0" max="100" value="${n.progress||0}"></div>`;if(n.type==='idea')return `<div class="field"><label>Источник</label><input name="source" value="${esc(n.source||'')}"></div>`;if(n.type==='goal')return `<div class="two"><div class="field"><label>Метрика</label><input name="metric" value="${esc(n.metric||'')}"></div><div class="field"><label>Прогресс</label><input name="progress" type="number" min="0" max="100" value="${n.progress||0}"></div></div>`;return ''}
export function createPicker(){return `<div class="overlay" data-close-modal><section class="sheet" data-modal><header class="sheet-head"><div><small>НОВАЯ СУЩНОСТЬ</small><h2>Что создать?</h2></div><button data-close>×</button></header><div class="sheet-body"><div class="click-list">${Object.entries(TYPE_LABELS).map(([k,v])=>`<button class="click-row" data-new-type="${k}"><span class="click-icon">${cardIcon(k)}</span><span><b>${v}</b><small>Создать карточку</small></span><span>›</span></button>`).join('')}</div></div></section></div>`}
